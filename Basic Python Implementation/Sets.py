
marks = {97, 94, 95, 95, 95}    # {} represents set
#print(marks)                   # set stores unique value, no repitition of item in output
                                # values can't be accessed through indices in sets, for traversal/iteration we'll use loop

for i in marks:
    print(i)

# sets are called unordered because it doesnot have indices